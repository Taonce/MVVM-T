<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="itemClickListener"
            type="com.taonce.mvvm.base.OnItemClickListener" />

        <variable
            name="itemLongClickListener"
            type="com.taonce.mvvm.base.OnItemLongClickListener" />

        <variable
            name="adapter"
            type="androidx.recyclerview.widget.RecyclerView.Adapter" />

        <variable
            name="loadMore"
            type="com.taonce.mvvm.base.OnLoadMoreListener" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rcv"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:adapter="@{adapter}"
            android:scrollbars="vertical"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            bind:itemClickListener="@{itemClickListener}"
            bind:itemLongClickListener="@{itemLongClickListener}"
            bind:loadMore="@{loadMore}"
            tools:itemCount="50"
            tools:listitem="@layout/recycler_item_main" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>